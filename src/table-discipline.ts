/**
 * 全局填表纪律（2026-08-10）：所有表格维护共用的执行层规则。
 *
 * 注入点：
 * - 回填提取器（table-backfill.ts buildTableBackfillPrompt 的 systemPrompt）；
 * - 物化（templates.ts materializeTemplate 的 description 前缀，场记/UI 可见）。
 *
 * 三个问题靶向（用户反馈）：
 * 1. 乱填信息不具实 —— 编造/脑补/推测补全；
 * 2. 信息隔离差 —— 人与人之间设定混用；
 * 3. 多表关联差 —— 把「关联」理解成「把多表内容合并填到一起」。
 *
 * 注意：只给泛化纪律，不写死任何表的优先级/顺序（各表自己的规则见其 description）。
 */
export const TABLE_DISCIPLINE = `【全局填表纪律】（优先级高于表规则之外的任何通用直觉）
1. 只填事实：只填写资料中明确出现的信息。某列信息缺失时只写「未知」二字（或按表说明允许的占位），严禁编造、脑补、按刻板印象推测补全，也严禁长篇解释「为什么未知」——未知就是未知，不展开、不凑字数；表说明的字数下限只约束实际写出的描述，不约束未知列。
2. 信息隔离：一行只记录一个人物/一个实体的信息，各列值只来自该实体自身。严禁把其他人物的外貌、身份、经历、态度、行为混入本行，也严禁把本行内容复制到他人行。人物以姓名+上下文唯一确定，同名不同人不得混淆。
3. 表间关联是引用不是搬运：需要与其他表保持一致/关联时（如「姓名与XX表一致」「地点与XX表一致」），只填写引用键值（姓名、地点名等），严禁把其他表的内容搬进本行、合并进本行，更不要把关联关系理解成「多表内容填到一处」。拿不准引用目标时宁可不写。`;
